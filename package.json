{
  "name": "@agennext/vision-ai",
  "version": "1.0.0",
  "description": "Secure unified computer vision API and marketing site for object detection, OCR, and image classification",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "bun run typecheck",
    "ci": "bun install --frozen-lockfile && bun run typecheck && bun test"
  },
  "dependencies": {
    "elysia": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
